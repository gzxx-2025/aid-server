'use client'

import ForgotPasswordPage from '@/components/forgot-password/ForgotPasswordPage'

/** 找回密码独立页：复用 (home) 侧栏，主区按蓝湖三步流程展示 */
export default function ForgotPasswordRoutePage() {
  return <ForgotPasswordPage />
}
