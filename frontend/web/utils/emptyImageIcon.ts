import emptyImageIcon from '~/assets/img/icon/empty_icon.svg'
import noDataIcon from '~/assets/img/icon/no_data.svg'
import { assetUrl } from '~/utils/assetUrl'

/** 全站缺省图 / 空状态占位 logo */
export const emptyImageIconUrl = assetUrl(emptyImageIcon)
export const noDataIconUrl = assetUrl(noDataIcon)
