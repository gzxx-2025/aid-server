# 页面标题
VITE_APP_TITLE = AID 管理系统

# 开发环境配置
VITE_ENV = development

# 接口前缀（与原 Vue 版保持一致，走 Vite 代理转到后端）
VITE_APP_BASE_API = /dev-api

# 真实后端地址（仅开发代理使用）
VITE_BACKEND_HOST = https://apinotify.aidstudio.com.cn
