# Runtime contract

- 根入口只做校验、澄清和调度，不直接调用 Provider。
- 子 Skill 固定不可变版本；内部调用只能收窄权限，不得递归成环。
- NEEDS_INPUT 不提前创建后续写作或媒体任务；若未来使用模型规划，仍须创建现有文本媒体任务并按既有规则计费。
- Run 只关联 aid_media_task。任务状态、金额、供应商响应、取消、补偿和退款以 aid_media_task 及现有账务为唯一事实源。
- 不输出、不持久化模型私有思维链。
