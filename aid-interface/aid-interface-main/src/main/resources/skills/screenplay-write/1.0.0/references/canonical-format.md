# AID canonical screenplay plaintext

输出是纯叙事文本。剧集第一行必须独占写 `本集正文`，电影第一行必须独占写 `电影正文`，两者都不带冒号。

每个场标题独占一行并使用：`场次 N：地点 内/外 日/夜`。N 从 1 递增。人物说话前，说话人独占一行并使用：`人物名：`。动作、环境和表演说明写在普通叙事行。

不得输出 JSON、Markdown 代码围栏、`正文：`、`第1场`、EP/SC 标记、审核意见、目标时长、版本号或其他元数据。解析器回归测试是最终契约。
