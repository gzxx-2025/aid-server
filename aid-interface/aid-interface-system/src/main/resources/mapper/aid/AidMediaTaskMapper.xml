<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aid.aid.mapper.AidMediaTaskMapper">

    <!--
      业务说明：
      1) 该 mapper 主要依赖 MyBatis-Plus 的通用 CRUD；
      2) 自定义查询见下方 statement。
    -->

    <select id="selectTasksForCompensation" resultType="com.aid.aid.domain.media.AidMediaTask">
        select *
        from aid_media_task
        where status = #{status}
          and provider_task_id is not null
          and provider_task_id != ''
          and (retry_count is null or retry_count &lt;= #{maxRetry})
          and (update_time is null or update_time &lt;= #{updateBefore})
        order by update_time asc, create_time asc
        limit #{limit}
    </select>

</mapper>

